# Variables de entorno para configuración crítica
# Copia este archivo a .env y completa los valores

# API Key de IMDB (obtener en https://imdb-api.com/api)
IMDB_API_KEY=your_imdb_api_key_here

# Token del bot de Telegram (obtener de @BotFather)
# Solo sirve para subir archivos de hasta 50MB (límite real del Bot API
# de Telegram para el envío de vídeos/documentos vía api.telegram.org).
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# ID del canal de Telegram (formato: @channel_name o -1001234567890)
TELEGRAM_CHANNEL_ID=your_telegram_channel_id_here

# Para subir archivos más grandes de 50MB, la app usa Telethon (un
# cliente de tu propia cuenta de Telegram, no del bot) — hace falta
# rellenar también estas tres. Consíguelas en https://my.telegram.org
# -> API development tools (créalas con tu cuenta personal, no con el
# bot). TELEGRAM_PHONE en formato internacional, p.ej. +34600000000.
TELEGRAM_API_ID=
TELEGRAM_API_HASH=
TELEGRAM_PHONE=

# Token de Plex (obtener desde la configuración de Plex)
PLEX_TOKEN=your_plex_token_here

# URL del servidor Plex (opcional, por defecto localhost:32400)
PLEX_URL=http://localhost:32400

# API Key de OMDb (gratis en https://www.omdbapi.com/apikey.aspx)
# Se usa para contrastar contra una base de datos real las sugerencias
# de nombre de la IA en el detector de huérfanos (si no está, la IA
# responde solo de memoria).
OMDB_API_KEY=

# API Key de TMDB (gratis en https://www.themoviedb.org/settings/api)
TMDB_API_KEY=

# Solo si eliges OpenAI o Gemini como proveedor de IA para nombres de
# huérfanos (Ollama en local no necesita ninguna de las dos)
OPENAI_API_KEY=
GEMINI_API_KEY=